"use strict"

var fs = require("fs");

// Read and parse JSON file synchonously
module.exports = function (fileName) {

    try {
        var json = JSON.parse(fs.readFileSync(fileName, 'utf8'));
        return (json);
    } catch (err) {
        return "";
    }
}
